<%# Button Modal %>
<button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#ModaleCreateAccomodation">
  Add an accomodation
</button>

<%# Corps Modal => Create %>
<div class="modal fade" id="ModaleCreateAccomodation" tabindex="-1" aria-labelledby="ModaleCreateAccomodationLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="ModaleCreateAccomodationLabel">Add your accomodation here</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>

      <%= simple_form_for [trip, accomodation] do |f| %>
        <div class="modal-body">
          <%= f.input :place_name %>
          <%= f.input :accomodation_type %>
          <%= f.date_field :start_date %>
          <%= f.date_field :end_date %>
          <%= f.input :address %>
          <%= f.input :price %>
        </div>
        <div class="modal-footer">
          <%= f.submit 'Add', class: 'btn btn-primary' %>
        </div>
      <% end %>
    </div>
  </div>
</div>
